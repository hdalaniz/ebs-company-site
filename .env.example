# Optional override for the EBS Insight origin (no trailing slash, no /analyze path).
# Current canonical default in config/site.ts: https://ebs-insight-v2.vercel.app
# After the clean domain is moved onto the V2 project, the code default should
# become https://ebs-insight.vercel.app again.
# Local example: http://localhost:3001
# NEXT_PUBLIC_EBS_INSIGHT_URL=
