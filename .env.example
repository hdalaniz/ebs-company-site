# Origin of the existing EBS Insight application (no trailing slash).
# Example for local Insight: http://localhost:3001
# Leave empty until the real staging/production origin is known.
# Do not invent a production URL.
NEXT_PUBLIC_EBS_INSIGHT_URL=
