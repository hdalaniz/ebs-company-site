# Optional override for the EBS Insight origin (no trailing slash, no /analyze path).
# Default in code: https://ebs-insight.vercel.app
# Local example: http://localhost:3001
# NEXT_PUBLIC_EBS_INSIGHT_URL=
